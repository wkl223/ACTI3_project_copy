package teammates.ui.controller;

import teammates.common.util.Const;
import teammates.common.util.StatusMessage;
import teammates.common.util.StatusMessageColor;


public class InstructorFeedbackResultsPageUploadAction extends Action {
    public ActionResult execute() {
        // file exception is needed?
        //1. read file, assign a key for this.
        //2. check previlege with logic class
        //3. return message from logic (done or not done)
        //4. display that message on the page as ActionResult
        return null;
    }
}
